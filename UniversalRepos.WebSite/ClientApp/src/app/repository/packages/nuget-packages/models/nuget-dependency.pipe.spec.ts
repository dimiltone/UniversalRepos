import { NugetDependencyPipe } from './nuget-dependency.pipe';

describe('NugetDependencyPipe', () => {
  it('create an instance', () => {
    const pipe = new NugetDependencyPipe();
    expect(pipe).toBeTruthy();
  });
});
